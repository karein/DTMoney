- Simular API
  - LIBS
    - json-server
    - Mirage JS 
    - msw

- Context 
  - compartilhamento de estados entre vários componentes da ap,licação independente de onde esses componentes (nível hieráquico) estejam

      - {/* forma antiga obter valores do context, com api(render props) do react  */}
        {/* 
          <TransactionsContext.Consumer>
          {(data) => {
            console.log(data)

            return <p>ok</p>
          }}
          </TransactionsContext.Consumer> 
        */}

      - /* forma atualizada de obter valores do context, com hook useContext do react  */
        import { useContext } from "react";
        import { TransactionsContext } from "../../transactionsContext";

        const data = useContext(TransactionsContext)


- ReactNode: aceita qualquer tipo de conteúdo válido do react

